# Interpolation variables for Docker Compose YAML
# Use this with --env-file during docker compose commands if needed,
# or copy to .env in the project root.

POSTGRES_USER=jules
POSTGRES_PASSWORD=password
POSTGRES_DB=sevra_atlas
REDIS_PASSWORD=
DOMAIN=localhost
