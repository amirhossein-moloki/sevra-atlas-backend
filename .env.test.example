# Test Environment Variables
NODE_ENV=test

# Database Configuration
# In docker-compose.test.yml, 'postgres-test' is the service name
DATABASE_URL="postgresql://jules:password@postgres-test:5432/sevra_atlas_test?schema=public"

# Redis Configuration
# In docker-compose.test.yml, 'redis-test' is the service name
REDIS_URL="redis://redis-test:6379"
REDIS_QUEUE_URL="redis://redis-test:6379"

# Authentication (JWT)
JWT_ACCESS_SECRET="test_access_secret"
JWT_REFRESH_SECRET="test_refresh_secret"
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=2592000

# SMS Provider
SMS_PROVIDER=mock

# Storage Provider
STORAGE_PROVIDER=local

# AdminJS Configuration
SESSION_SECRET="test_session_secret"

# Server Configuration
PORT=3000
