# Sevra Proxy Environment Configuration
DOMAIN=sevra.example.com
DOMAIN_WWW=www.sevra.example.com
EMAIL=admin@example.com

# Use api:3000 if running in Docker (root docker-compose), or host.docker.internal:3000
APP_UPSTREAM=api:3000
