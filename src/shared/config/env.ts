import { config } from '../../config';

/**
 * @deprecated Use import { config } from '@/config' instead.
 * This is maintained for backward compatibility during the configuration refactor.
 */
export const env = config;
